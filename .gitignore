.DS_Store
*.sqlite
node_modules/
dist/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# graft's LLM provider settings, including the API key — never commit this.
/.env

# personal Claude Code overrides (e.g. statusline preference) — not shared.
/.claude/settings.local.json
